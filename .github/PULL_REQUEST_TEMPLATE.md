## Summary

<!-- Compose / deploy / docs changes only — UI work belongs in letta-vision-client -->

## Test plan

- [ ] `docker compose config`
- [ ] `docker compose up -d --build` (clean `.env` from `.env.example`)
- [ ] Updated [CHANGELOG.md](../CHANGELOG.md) under **Unreleased**
